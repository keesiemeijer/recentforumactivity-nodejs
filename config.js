var config = {};

config.max_pages = 20;
config.port = 5000;

// open this app in the default browser.
// default: ''; Empty string. Don't open the browser.
// example: 'http://localhost:5000/profile/your-profile';
config.open = '';

module.exports = config;